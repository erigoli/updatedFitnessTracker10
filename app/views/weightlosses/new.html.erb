<!-- This heading indicates the section for adding weight loss -->
<h1>Add Weightloss</h1>

<!-- This form_with helper generates a form for adding weight loss -->
<%= form_with(model: @weightloss, url: weightlosses_path, local: true) do |form| %>

  <!-- This label prompts the user to enter their weight loss -->
  <%= form.label :value, "Enter Your Weightloss:" %>

  <!-- This number_field generates an input field for entering the weight loss -->
  <%= form.number_field :value %>

  <!-- This submit button allows the user to submit their weight loss -->
  <%= form.submit "Submit lbs" %>

<% end %>
